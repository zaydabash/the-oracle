# Database Configuration
# Quick try (SQLite):
DATABASE_URL=sqlite:///oracle.db
# Real demo (Postgres via docker):
# DATABASE_URL=postgresql+psycopg2://oracle:oracle@db:5432/oracle

# Oracle Mode: mock or live
ORACLE_MODE=mock
ORACLE_ADMIN_KEY=dev123

# Data Source Configuration
ARXIV_CATEGORIES=cs.AI,cs.CL,cs.LG,stat.ML,cs.RO,cs.CV
GITHUB_TOKEN=
CRUNCHBASE_API_KEY=
JOBS_FEED_URLS=https://weworkremotely.com/categories/remote-programming-jobs.rss,https://remoteok.io/remote-dev-jobs.rss

# Topic Configuration
TOPIC_KEYWORDS_JSON=./data/topic_keywords.json

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Forecasting
FORECAST_HORIZONS=30,90,180
SURGE_SCORE_WEIGHTS={"velocity_growth": 0.4, "momentum": 0.3, "z_spike": 0.2, "convergence": 0.1}

# Development
DEBUG=true
RELOAD=true
